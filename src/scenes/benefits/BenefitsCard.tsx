import React from 'react';

type Props = {
  data: {
    icon: JSX.Element;
    title: string;
    description: string;
  };
};

export default function BenefitsCard({ data }: Props) {
  return <div>BenefitsCard</div>;
}
